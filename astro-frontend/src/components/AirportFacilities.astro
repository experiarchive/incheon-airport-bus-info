---
---


<div id="airport-facilities-container" class="mt-8 mb-8 bg-white dark:bg-gray-800 rounded-lg shadow-md border border-gray-200 dark:border-gray-700 p-6 transition-all duration-300">
  
  <div class="flex items-center justify-between mb-6 flex-wrap gap-2">
    <h3 class="text-xl font-bold flex items-center gap-2 text-gray-800 dark:text-white">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-purple-600 dark:text-purple-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
      </svg>
      인천공항 편의시설 안내
    </h3>
    
    <!-- Top Level Filters (Always Visible) -->
    <div class="flex gap-2">
      <div class="flex bg-gray-100 dark:bg-gray-700 rounded-lg p-1">
        <button class="terminal-toggle px-3 py-1.5 text-xs font-semibold rounded-md transition-all text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white" data-value="">전체</button>
        <button class="terminal-toggle px-3 py-1.5 text-xs font-semibold rounded-md transition-all text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white active-toggle bg-white dark:bg-gray-600 shadow-sm" data-value="P01">제1터미널</button>
        <button class="terminal-toggle px-3 py-1.5 text-xs font-semibold rounded-md transition-all text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white" data-value="P03">제2터미널</button>
      </div>
      <div class="flex bg-gray-100 dark:bg-gray-700 rounded-lg p-1">
        <button class="area-toggle px-3 py-1.5 text-xs font-semibold rounded-md transition-all text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white active-toggle bg-white dark:bg-gray-600 shadow-sm" data-value="">전체</button>
        <button class="area-toggle px-3 py-1.5 text-xs font-semibold rounded-md transition-all text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white" data-value="N">일반</button>
        <button class="area-toggle px-3 py-1.5 text-xs font-semibold rounded-md transition-all text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white" data-value="Y">면세</button>
      </div>
    </div>
  </div>

  <!-- Loading State -->
  <div id="facilities-loading" class="flex flex-col items-center justify-center py-12">
    <div class="animate-spin rounded-full h-10 w-10 border-b-2 border-purple-600"></div>
    <p class="mt-4 text-sm text-gray-500 dark:text-gray-400">편의시설 정보를 불러오는 중입니다...</p>
  </div>

  <!-- VIEW 1: Category Selection Grid (Initial View) -->
  <div id="category-view" class="hidden animate-fade-in">
    <p class="text-center text-gray-600 dark:text-gray-300 mb-6">찾으시는 편의시설 종류를 선택해주세요.</p>
    <div id="category-grid" class="grid grid-cols-2 md:grid-cols-4 gap-4">
      <!-- Generated via JS -->
    </div>
  </div>

  <!-- VIEW 2: Results List (Filtered View) -->
  <div id="list-view" class="hidden animate-fade-in">
    <!-- List Header / Back Button -->
    <div class="flex items-center justify-between mb-4 bg-purple-50 dark:bg-purple-900/20 p-3 rounded-lg border border-purple-100 dark:border-purple-800">
      <button id="back-to-categories" class="flex items-center text-sm font-semibold text-purple-700 dark:text-purple-300 hover:text-purple-900 dark:hover:text-purple-100">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
        카테고리 다시 선택
      </button>
      <div class="flex items-center gap-2">
        <span id="current-category-badge" class="px-2 py-1 bg-white dark:bg-purple-800 text-purple-700 dark:text-purple-200 rounded text-xs font-bold shadow-sm">
          전체
        </span>
        <span id="result-count" class="text-xs text-gray-500 dark:text-gray-400"></span>
      </div>
    </div>

    <!-- Search Bar -->
    <div class="relative mb-4">
      <input type="text" id="facility-search" placeholder="결과 내 시설명 검색..." class="w-full pl-10 pr-4 py-2 border border-gray-200 dark:border-gray-600 rounded-lg bg-gray-50 dark:bg-gray-700 text-sm focus:ring-2 focus:ring-purple-300 dark:focus:ring-purple-500 focus:outline-none dark:text-white" />
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-400 absolute left-3 top-2.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
      </svg>
    </div>

    <!-- Results Grid -->
    <div id="results-grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 min-h-[200px]">
      <!-- Cards Injected Here -->
    </div>
    
    <!-- Empty State for List -->
    <div id="list-empty" class="hidden text-center py-10 bg-gray-50 dark:bg-gray-700/30 rounded-lg border border-dashed border-gray-200 dark:border-gray-600">
      <p class="text-gray-500 dark:text-gray-400">이 카테고리에는 해당 조건의 시설이 없습니다.</p>
      <button id="reset-filters-btn" class="mt-2 text-sm text-purple-600 dark:text-purple-400 underline hover:text-purple-800 dark:hover:text-purple-300">필터 초기화</button>
    </div>
  </div>

  <div class="text-right mt-6 pt-4 border-t border-gray-100 dark:border-gray-700">
    <p class="text-[10px] text-gray-400 dark:text-gray-500">데이터 제공: 인천국제공항공사 (실시간 API)</p>
  </div>
</div>

<style>
  .active-toggle {
    background-color: white;
    box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    color: #4b5563; /* gray-600 */
    border: 1px solid #e5e7eb; /* gray-200 */
  }
  
  :global(.dark) .active-toggle {
    background-color: #4b5563; /* gray-600 */
    color: white;
    border: 1px solid #374151; /* gray-700 */
  }

  /* Custom Scrollbar for list view if needed */
  #results-grid::-webkit-scrollbar {
    width: 6px;
  }
  #results-grid::-webkit-scrollbar-thumb {
    background-color: #d1d5db;
    border-radius: 4px;
  }
</style>

<script>
  // State
  let allFacilities = [];
  let currentTerminal = 'P01'; // Default to T1
  let currentArea = '';       // Default All
  let currentCategory = null; // null = Show Categories View
  let searchQuery = '';
  // Pagination State
  let currentPage = 1;
  const ITEMS_PER_PAGE = 12;

  // Icon Mapping (Unchanged)
  const CATEGORY_MAP = {
    '식음료': { icon: '🍽️', label: '식당/카페', keywords: ['식당', '카페', '베이커리', '식음료', '패스트푸드'] },
    '쇼핑': { icon: '🛍️', label: '면세점/쇼핑', keywords: ['면세점', '쇼핑', '편의점', '의류', '화장품', '잡화', '서점'] },
    '안내': { icon: 'ℹ️', label: '안내/서비스', keywords: ['안내', '로밍', '통신', '환전', '은행', '보험', '택배', '세탁'] },
    '의료': { icon: '💊', label: '의료/약국', keywords: ['약국', '병원', '의료'] },
    '휴식': { icon: '🛋️', label: '휴식/라운지', keywords: ['라운지', '휴식', '호텔', '스파'] },
    '편의': { icon: '🚽', label: '편의시설', keywords: ['화장실', '흡연실', '샤워실', '기도실', '유아휴게실', '키즈'] },
    '교통': { icon: '🚌', label: '교통/주차', keywords: ['주차', '렌터카', '발렛', '교통'] },
    '기타': { icon: '🔎', label: '기타 시설', keywords: [] }
  };

  async function init() {
    await fetchFacilities();
    setupEventListeners();
    render();
  }

  async function fetchFacilities() {
    try {
      const res = await fetch('/api/facilities');
      if (res.ok) {
        const json = await res.json();
        if (json.response?.body?.items) {
          allFacilities = json.response.body.items;
          document.getElementById('facilities-loading').classList.add('hidden');
        } else {
             allFacilities = [];
             document.getElementById('facilities-loading').innerHTML = '<p>데이터가 없습니다.</p>';
        }
      }
    } catch (e) {
      console.error('Failed to fetch facilities:', e);
      document.getElementById('facilities-loading').innerHTML = '<p class="text-red-500">데이터를 불러올 수 없습니다.</p>';
    }
  }

  function getGroupedCategory(apiCategory) {
    if (!apiCategory) return '기타';
    for (const [key, config] of Object.entries(CATEGORY_MAP)) {
      if (key === '기타') continue;
      if (apiCategory.includes(key) || config.keywords.some(k => apiCategory.includes(k))) {
        return key;
      }
    }
    return '기타';
  }

  function getAvailableCategories() {
    const contextItems = allFacilities.filter(f => {
      if (currentTerminal && f.terminal !== currentTerminal) return false;
      if (currentArea && f.area !== currentArea) return false;
      return true;
    });

    const groups = new Set();
    contextItems.forEach(f => {
      groups.add(getGroupedCategory(f.category));
    });

    return Object.keys(CATEGORY_MAP).filter(key => groups.has(key));
  }

  function render() {
    const categoryView = document.getElementById('category-view');
    const listView = document.getElementById('list-view');

    document.querySelectorAll('.terminal-toggle').forEach(btn => {
      if (btn.dataset.value === currentTerminal) btn.classList.add('active-toggle', 'bg-white', 'dark:bg-gray-600', 'shadow-sm');
      else btn.classList.remove('active-toggle', 'bg-white', 'dark:bg-gray-600', 'shadow-sm');
    });
    document.querySelectorAll('.area-toggle').forEach(btn => {
      if (btn.dataset.value === currentArea) btn.classList.add('active-toggle', 'bg-white', 'dark:bg-gray-600', 'shadow-sm');
      else btn.classList.remove('active-toggle', 'bg-white', 'dark:bg-gray-600', 'shadow-sm');
    });

    if (currentCategory) {
      categoryView.classList.add('hidden');
      listView.classList.remove('hidden');
      renderList();
    } else {
      listView.classList.add('hidden');
      categoryView.classList.remove('hidden');
      renderCategories();
    }
  }

  function renderCategories() {
    const grid = document.getElementById('category-grid');
    const available = getAvailableCategories();

    if (available.length === 0) {
      grid.innerHTML = '<div class="col-span-full text-center py-8 bg-gray-50 dark:bg-gray-700/50 rounded-lg"><p class="text-gray-500 dark:text-gray-400">해당 조건에 맞는 시설이 없습니다.</p></div>';
      return;
    }

    grid.innerHTML = available.map(key => {
      const config = CATEGORY_MAP[key];
      const count = allFacilities.filter(f => {
        if (currentTerminal && f.terminal !== currentTerminal) return false;
        if (currentArea && f.area !== currentArea) return false;
        return getGroupedCategory(f.category) === key;
      }).length;

      return `
        <button 
          onclick="selectCategory('${key}')"
          class="flex flex-col items-center justify-center p-6 bg-white dark:bg-gray-700 border border-gray-100 dark:border-gray-600 rounded-xl shadow-sm hover:shadow-md hover:border-purple-300 dark:hover:border-purple-500 hover:-translate-y-1 transition-all duration-200 group"
        >
          <span class="text-3xl mb-3 group-hover:scale-110 transition-transform">${config.icon}</span>
          <span class="font-bold text-gray-800 dark:text-gray-100 text-sm md:text-base">${config.label}</span>
          <span class="text-xs text-gray-400 dark:text-gray-500 mt-1">${count}곳</span>
        </button>
      `;
    }).join('');
  }

  function renderList() {
    const grid = document.getElementById('results-grid');
    const badge = document.getElementById('current-category-badge');
    const countEl = document.getElementById('result-count');
    const empty = document.getElementById('list-empty');
    let loadMoreContainer = document.getElementById('load-more-container'); // Check if exists

    // Create Load More container if it doesn't exist
    if (!loadMoreContainer) {
      loadMoreContainer = document.createElement('div');
      loadMoreContainer.id = 'load-more-container';
      loadMoreContainer.className = 'col-span-full text-center mt-6 hidden';
      loadMoreContainer.innerHTML = `
        <button id="load-more-btn" class="px-6 py-2.5 bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 text-sm font-semibold rounded-full transition-colors flex items-center justify-center mx-auto gap-2">
          <span>더 보기</span>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </button>
      `;
      grid.parentNode.appendChild(loadMoreContainer);
      
      // Bind event listener properly
      document.getElementById('load-more-btn').addEventListener('click', () => {
        currentPage++;
        renderList();
      });
    }

    const config = CATEGORY_MAP[currentCategory];
    badge.textContent = config ? config.label : '전체';

    const allFilteredItems = allFacilities.filter(f => {
      if (currentTerminal && f.terminal !== currentTerminal) return false;
      if (currentArea && f.area !== currentArea) return false;
      if (currentCategory !== '기타' && getGroupedCategory(f.category) !== currentCategory) return false;
      if (currentCategory === '기타' && getGroupedCategory(f.category) !== '기타') return false;
      if (searchQuery && !f.name.toLowerCase().includes(searchQuery)) return false;
      return true;
    });

    countEl.textContent = `총 ${allFilteredItems.length}개`;

    if (allFilteredItems.length === 0) {
      grid.innerHTML = '';
      grid.classList.add('hidden');
      empty.classList.remove('hidden');
      loadMoreContainer.classList.add('hidden');
      return;
    }

    grid.classList.remove('hidden');
    empty.classList.add('hidden');

    // Pagination Logic
    const displayCount = currentPage * ITEMS_PER_PAGE;
    const displayItems = allFilteredItems.slice(0, displayCount);
    
    // Generate HTML
    const cardsHtml = displayItems.map(f => {
      const areaLabel = f.area === 'Y' ? '면세' : '일반';
      const areaColor = f.area === 'Y' ? 'bg-amber-100 dark:bg-amber-900/30 text-amber-700 dark:text-amber-300' : 'bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-300';
      const terminalLabel = f.terminal === 'P01' ? 'T1' : (f.terminal === 'P03' ? 'T2' : '');

      return `
        <div class="border border-gray-100 dark:border-gray-700 rounded-lg p-4 hover:shadow-md transition-shadow bg-white dark:bg-gray-700 h-full flex flex-col animate-fade-in">
          <div class="flex items-start justify-between gap-2 mb-2">
            <h4 class="font-bold text-gray-800 dark:text-grey-100 text-sm leading-tight dark:text-white">${f.name || '-'}</h4>
            <div class="flex gap-1 flex-shrink-0">
               ${terminalLabel ? `<span class="text-[10px] px-1.5 py-0.5 rounded bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 border border-blue-200 dark:border-blue-800">${terminalLabel}</span>` : ''}
              <span class="text-[10px] px-1.5 py-0.5 rounded border border-transparent ${areaColor}">${areaLabel}</span>
            </div>
          </div>
          <p class="text-xs text-gray-500 dark:text-gray-400 mb-3 line-clamp-2 min-h-[2.5em]">${f.location || '위치 정보 없음'}</p>
          
          <div class="mt-auto pt-3 border-t border-gray-50 dark:border-gray-600 flex flex-col gap-1.5">
            <div class="flex items-center text-xs text-gray-600 dark:text-gray-300">
               <span class="w-4 flex justify-center mr-1.5">🕒</span>
               <span class="truncate">${f.servicetime || '운영시간 정보 없음'}</span>
            </div>
            ${f.tel ? `
            <div class="flex items-center text-xs text-gray-600 dark:text-gray-300">
               <span class="w-4 flex justify-center mr-1.5">📞</span>
               <a href="tel:${f.tel}" class="hover:text-purple-600 dark:hover:text-purple-400 underline decoration-dotted">${f.tel}</a>
            </div>` : ''}
             ${f.items ? `
             <div class="flex items-center text-xs text-gray-500 dark:text-gray-400 mt-1">
               <span class="w-4 flex justify-center mr-1.5">🏷️</span>
               <span class="truncate">${f.items}</span>
            </div>` : ''}
          </div>
        </div>
      `;
    }).join('');

    grid.innerHTML = cardsHtml;

    // Show/Hide Load More Button
    if (allFilteredItems.length > displayCount) {
      loadMoreContainer.classList.remove('hidden');
      // Update button text to show remaining count
      const remaining = allFilteredItems.length - displayCount;
      const btnSpan = loadMoreContainer.querySelector('span');
      if (btnSpan) btnSpan.textContent = `더 보기 (${remaining}개)`;
    } else {
      loadMoreContainer.classList.add('hidden');
    }
  }

  function setupEventListeners() {
    document.querySelectorAll('.terminal-toggle').forEach(btn => {
      btn.addEventListener('click', (e) => {
        currentTerminal = e.target.dataset.value;
        currentPage = 1; // Reset pagination
        render(); 
      });
    });
    
    document.querySelectorAll('.area-toggle').forEach(btn => {
      btn.addEventListener('click', (e) => {
        currentArea = e.target.dataset.value;
        currentPage = 1; // Reset pagination
        render();
      });
    });

    document.getElementById('back-to-categories').addEventListener('click', () => {
      currentCategory = null;
      searchQuery = '';
      currentPage = 1; // Reset pagination
      document.getElementById('facility-search').value = '';
      render();
    });

    document.getElementById('facility-search').addEventListener('input', (e) => {
      searchQuery = e.target.value.toLowerCase();
      currentPage = 1; // Reset pagination
      renderList();
    });

    document.getElementById('reset-filters-btn').addEventListener('click', () => {
      document.getElementById('facility-search').value = '';
      searchQuery = '';
      currentPage = 1; // Reset pagination
      renderList();
    });

    window.selectCategory = (key) => {
      currentCategory = key;
      currentPage = 1; // Reset pagination
      render();
    };
  }

  // Auto Init
  init();
</script>
