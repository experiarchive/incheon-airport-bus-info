---
import '../styles/global.css'; // Tailwind CSS 전역 스타일 임포트
import { Image } from 'astro:assets';
import websiteLogo from '../assets/website-logo.png';

// Props 정의 (예: 페이지 제목)
export interface Props {
	title: string;
	description?: string;
	keywords?: string;
	ogImage?: string;
	ogType?: string;
	canonical?: string;
}
const { 
	title, 
	description = "인천공항 공항버스 시간표, 요금, 정류장 정보를 실시간으로 확인하고 온라인 예매까지! 서울 지역 모든 노선의 상세 정보를 제공합니다.",
	keywords = "공항버스, 인천공항, 공항버스 시간표, 공항버스 예매, 공항버스 요금, 6001, 6002, 6003, 6004, 6005, 6006, 6007, 6008, 6009, 6010, 6011, 6012, 6013, 6014, 6015, 6016, 6017, 6018, 6019, 6020, 6006, 6009, 6200, 6300, 6500, 6600, 6701, 6702, 6703, 6705A, 6100, 6102, 6103, 6104", // <--- 여기에 콤마를 추가하세요!
	// 기본 OG 이미지 설정: public/default-og-image.png 파일을 1200×630px JPG/PNG로 교체하세요
	ogImage = "/default-og-image.png",
	ogType = "website",
	canonical
} = Astro.props;

// 현재 URL 생성
const currentUrl = new URL(Astro.url.pathname, Astro.site);
const canonicalUrl = canonical ? new URL(canonical, Astro.site) : currentUrl;
---

<!doctype html>
<html lang="ko">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		
		<!-- SEO 메타 태그 -->
		<title>{title}</title>
		<meta name="description" content={description} />
		<meta name="keywords" content={keywords} />
		<link rel="canonical" href={canonicalUrl} />
		
		<!-- Open Graph 메타 태그 -->
		<meta property="og:type" content={ogType} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={new URL(ogImage, Astro.site)} />
		<meta property="og:url" content={currentUrl} />
		<meta property="og:site_name" content="인천공항 버스 정보" />
		<meta property="og:locale" content="ko_KR" />
		
		<!-- Google AdSense -->
		<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-8355312170222120"
     crossorigin="anonymous"></script>
		
		<!-- Twitter Card 메타 태그 -->
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={new URL(ogImage, Astro.site)} />
		
		<!-- 추가 SEO 메타 태그 -->
		<meta name="robots" content="index, follow" />
		<meta name="author" content="인천공항 버스 정보" />
		<meta name="theme-color" content="#2563eb" />
		
		<!-- Pretendard 웹폰트 -->
		<link rel="preconnect" href="https://cdn.jsdelivr.net">
		<link rel="stylesheet" as="style" crossorigin href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css" />
		
		<!-- 구조화된 데이터 (JSON-LD) -->
		<script type="application/ld+json">
		{
			"@context": "https://schema.org",
			"@type": "WebSite",
			"name": "인천공항 버스 정보",
			"url": "https://air.koreabusinfo.com",
			"description": "인천공항 공항버스 시간표, 요금, 정류장 정보 제공",
			"potentialAction": {
				"@type": "SearchAction",
				"target": "https://air.koreabusinfo.com/routes?search={search_term_string}",
				"query-input": "required name=search_term_string"
			}
		}
		</script>
		
		<style>
			/* 여기에 아주 기본적인 전역 스타일이나 레이아웃 스타일을 추가할 수 있지만,
			대부분은 Tailwind CSS 클래스로 처리하거나 global.css에 정의합니다. */
			body {
				display: flex;
				flex-direction: column;
				min-height: 100vh;
				font-family: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, system-ui, Roboto, "Helvetica Neue", "Segoe UI", "Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", sans-serif;
			}
			main {
				flex-grow: 1;
			}
		</style>
	</head>
	<body class="bg-gray-100 text-gray-800 dark:bg-gray-900 dark:text-gray-200">
		<header class="bg-white text-gray-800 shadow-md sticky top-0 z-50 border-b border-gray-200">
			<nav class="container mx-auto px-4 sm:px-6 py-3">
				<div class="flex items-center justify-between">
					<!-- 로고/사이트명 -->
					<div class="flex items-center">
						<a href="/" class="hover:opacity-80 transition duration-200">
							<Image 
								src={websiteLogo} 
								alt="인천공항 공항버스 정보 - 실시간 시간표 및 예매 서비스" 
								class="h-10 w-auto"
								loading="eager"
							/>
						</a>
					</div>

					<!-- 데스크톱 네비게이션 메뉴 -->
					<div class="hidden md:flex items-center space-x-1">
						<a href="/routes" class="px-4 py-2 rounded-lg hover:bg-gray-100 transition duration-200 font-medium text-gray-700 hover:text-blue-600">
							전체 노선
						</a>
						<a href="/info" class="px-4 py-2 rounded-lg hover:bg-gray-100 transition duration-200 font-medium text-gray-700 hover:text-blue-600">
							공항 정보
						</a>
					</div>

					<!-- 모바일 햄버거 메뉴 버튼 -->
					<button 
						id="mobile-menu-button"
						class="md:hidden p-2 rounded-lg hover:bg-gray-100 transition duration-200 text-gray-600"
						aria-label="메뉴 열기"
						aria-expanded="false"
					>
						<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
							<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
						</svg>
					</button>
				</div>

				<!-- 모바일 네비게이션 메뉴 -->
				<div id="mobile-menu" class="md:hidden mt-3 pb-3 border-t border-gray-200 hidden">
					<div class="flex flex-col space-y-2 pt-3">
						<a href="/routes" class="block px-4 py-3 rounded-lg hover:bg-gray-100 transition duration-200 font-medium text-gray-700 hover:text-blue-600">
							📍 전체 노선
						</a>
						<a href="/info" class="block px-4 py-3 rounded-lg hover:bg-gray-100 transition duration-200 font-medium text-gray-700 hover:text-blue-600">
							ℹ️ 공항 정보
						</a>
					</div>
				</div>
			</nav>
		</header>

		<main class="container mx-auto px-4 sm:px-6 py-6 sm:py-8">
			<slot /> {/* 페이지별 콘텐츠가 여기에 렌더링됩니다. */}
		</main>

		<footer class="bg-gray-800 text-white text-center p-4 mt-auto">
			<p>© {new Date().getFullYear()} 인천공항 버스 정보. All rights reserved.</p>
		</footer>

		<!-- 모바일 메뉴 토글 스크립트 -->
		<script>
			document.addEventListener('DOMContentLoaded', function() {
				const menuButton = document.getElementById('mobile-menu-button');
				const mobileMenu = document.getElementById('mobile-menu');
				
				if (menuButton && mobileMenu) {
					menuButton.addEventListener('click', function() {
						const isExpanded = menuButton.getAttribute('aria-expanded') === 'true';
						
						// 메뉴 토글
						mobileMenu.classList.toggle('hidden');
						
						// 접근성 속성 업데이트
						menuButton.setAttribute('aria-expanded', (!isExpanded).toString());
						menuButton.setAttribute('aria-label', isExpanded ? '메뉴 열기' : '메뉴 닫기');
						
						// 햄버거 아이콘 변경
						const svg = menuButton.querySelector('svg');
						if (svg) {
							if (isExpanded) {
								// 햄버거 아이콘으로 변경
								svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>';
							} else {
								// X 아이콘으로 변경
								svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>';
							}
						}
					});

					// 메뉴 외부 클릭 시 메뉴 닫기
					document.addEventListener('click', function(event) {
						const target = event.target;
						if (target && target instanceof Element) {
							if (!menuButton.contains(target) && !mobileMenu.contains(target)) {
								mobileMenu.classList.add('hidden');
								menuButton.setAttribute('aria-expanded', 'false');
								menuButton.setAttribute('aria-label', '메뉴 열기');
								
								const svg = menuButton.querySelector('svg');
								if (svg) {
									svg.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>';
								}
							}
						}
					});
				}
			});
		</script>
	</body>
</html>
